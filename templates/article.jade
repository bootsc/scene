
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block header
  include author
  h1= page.title
  p.author
    | #{ "Photos by " }
    mixin author(page.metadata.photographer)
    | #{ " | Words by " }
    mixin author(page.metadata.author)

block cover
 img(src=locals.cdn + page.metadata.cover).cover

block content
  div.content-wrap
    article.article
      section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) « Home


